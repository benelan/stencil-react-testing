/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ["@stencil/core"],
  // webpack(config) {
  //   config.infrastructureLogging = {
  //     level: "error",
  //   };
  // },
};

module.exports = nextConfig;

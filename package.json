{
  "name": "stencil-react-vitetest",
  "private": true,
  "workspaces": [
    "packages/*",
    "app"
  ],
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "lerna run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "nx": {
    "targets": {
      "build": {
        "dependsOn": [
          "^build"
        ]
      }
    }
  },
  "devDependencies": {
    "lerna": "^7.1.5",
    "typescript": "^4.9.5"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "npm@8.19.4",
  "volta": {
    "node": "16.20.0"
  }
}
